name: flutter_application_2
description: A new Flutter project

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # ✅ UI Components
  getwidget: ^7.0.0   # versi terbaru, sudah support Flutter terbaru

  # ✅ Plugins
  device_info_plus: ^12.1.0
  shared_preferences: ^2.3.3
  intl: ^0.20.2
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/bg.png
    - assets/images/logo1.png
