name: flutter_application_2
description: A new Flutter project

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shimmer: ^3.0.0

  # 🔽 Tambahin 5 library ini
  getwidget: ^4.0.0
  device_info_plus: ^11.0.0
  shared_preferences: ^2.2.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/bg.png
    - assets/images/logo1.png
